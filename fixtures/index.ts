/**
 * Shared test fixtures for unit tests (MSW) and E2E tests (page.route).
 * Single source of truth for all mock data.
 *
 * @example
 *   import { mockRaindrops, mockCollections, mockUser, mockTags } from '@fixtures'
 */
export { mockRaindrops } from './raindrops'
export { mockCollections, mockChildCollections } from './collections'
export { mockUser } from './user'
export { mockTags } from './tags'
